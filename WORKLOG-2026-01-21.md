Worklog - 2026-01-21

- เพิ่มเมนูแอปบนหน้า `app/customer/page.tsx` ให้ใช้ข้อมูลเดียวกับเมนู header บนเว็บ พร้อม overlay mobile, active state, และ role admin.
- ปรับเมนูบน desktop ใน `app/customer/page.tsx` ให้ผูกกับชุดเมนูเดียวกัน.
- เอาช่องค้นหาใน `app/page.tsx` ออก แล้วคืนกลับตามที่ขอ (คืน state/filter/input และโฟกัส).
- ลองรัน `npm run dev` แต่ติด lock จาก next dev ตัวเดิม.
- ตรวจสอบ 401 Unauthorized จาก /api/quote-approvals และชี้ว่าตรวจเพียงคุกกี้ pin_auth ทำให้หลังกรอก PIN ถูกครั้งแรกยังโหลดได้จนคุกกี้หมดอายุ.

ค้าง/ต่อพรุ่งนี้:
- ไม่มี




