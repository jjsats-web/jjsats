# Worklog - 2025-12-26

- เพิ่มการดึง/แสดงลายเซ็นจากตอนลงทะเบียนใน PDF พร้อมชื่อในวงเล็บ และเพิ่มช่องแก้ไขลายเซ็นในหน้า “จัดการ PIN”
- ปรับเมนู header ให้มีลิงก์ “จัดการ PIN” ข้าง “ลงทะเบียน”
- ปรับบล็อกลายเซ็นฝั่งขวาใน PDF ให้เป็นแบบอนุมัติจัดซื้อจัดจ้าง
- เปลี่ยนการ Export PDF เป็นสร้างไฟล์ PDF ฝั่ง client (html2canvas + jsPDF) เพื่อไม่ให้มี header/footer ของเบราว์เซอร์
- อัปเดต dependency จาก npm audit fix และแก้ lint ใช้ next/image แทน img
- ปรับเลย์เอาต์ PDF ให้กึ่งกลางมากขึ้น, กันข้อความลายเซ็นตัดหน้า, และปรับหัวตารางให้อยู่กึ่งกลางทุกทิศทาง
