# Worklog: 2026-05-21

## ภาพรวม (Overview)
วันนี้ได้ทำการปรับปรุงและแก้ไขโครงสร้างหน้าเว็บหลายจุด เพื่อให้การแสดงผลมีความสวยงาม ทันสมัย และแก้ไขปัญหาต่างๆ ที่ผู้ใช้แจ้งมา โดยเน้นไปที่หน้าระบบใบเสนอราคา (Quotation) และ Header Navigation

## สิ่งที่ดำเนินการไป (Tasks Completed)

### 1. Refactor หน้า Quotation (`app/quotation/page.tsx`)
- รับโครงสร้าง HTML และ Tailwind CSS จากผู้ใช้งานมาแปลงเป็น Next.js React Component
- เปลี่ยนจาก CSS แบบเก่าที่ใช้ BEM มาใช้ Tailwind Utility Classes แบบเต็มรูปแบบ
- จัดวางโครงสร้าง `<AppHeader>` และ `<BottomNav>` เข้าไปใน Layout ใหม่
- เปลี่ยน HTML Tag ธรรมดาให้เป็น JSX Elements อย่างถูกต้อง (เช่น ใส่ `className` แทน `class`, ใช้ self-closing tags)
- ป้องกันปัญหาการแสดงผลภาษาไทยผิดเพี้ยน (Mojibake) โดยรักษาการเข้ารหัส (Encoding) ให้ถูกต้อง

### 2. ปรับปรุง `AppHeader` Component (`components/AppHeader.tsx`)
- อัปเดตโครงสร้างของ Header ให้ใช้โค้ด Tailwind ตามรูปแบบที่แนบมา (Fixed top, พื้นหลังสีขาว, จัดตำแหน่งต่างๆ แบบ Flexbox)
- เพิ่ม Profile Section และไอคอนการแจ้งเตือน (Notifications) ให้ตรงกับ Design
- ปรับ Active State ของเมนู (เพิ่มขีดเส้นใต้ และทำตัวหนา) ให้แสดงผลได้อย่างชัดเจน

### 3. ปรับระยะห่าง Layout ทั่วทั้งโปรเจกต์ (Global Padding Adjustments)
- ผู้ใช้งานพบปัญหาพื้นที่หน้าเว็บโดน Header บัง จึงมีการปรับระยะ `padding-top` ของแท็ก `<main>`
- เริ่มต้นจากทดลองปรับจาก `pt-6` เป็น `pt-24` ในหน้า Quotation
- สุดท้ายมีการสรุปให้ใช้ `pt-16` เป็นมาตรฐานเดียวกันทุกหน้า
- ทำการไล่ค้นหาและแก้โค้ด `<main>` ในทุกหน้าของระบบที่มี `page.tsx` (รวม 8 หน้า เช่น `app/customer/page.tsx`, `app/product/page.tsx`, `app/test/page.tsx`, ฯลฯ) ให้ใช้ `pt-16`

### 4. แก้ไขปัญหาเมนู "เสนอราคา2" หายไป
- พบว่ามีปัญหาเมนู "เสนอราคา2" แสดงอยู่แค่หน้าเดียว พอสลับหน้าอื่นแล้วเมนูหาย
- ตรวจสอบพบว่าโค้ดเมนูถูกประกาศอาร์เรย์ `menuItems` แยกกันไว้ในแต่ละหน้า (ไม่ได้ดึงจากที่เดียว)
- ทำการไล่เพิ่ม `{ id: "quote2", href: "/quotation", label: "เสนอราคา2", icon: "description" }` ลงในทุกๆ หน้าที่มีการประกาศ `menuItems` (เช่น `app/customer/page.tsx`, `app/product/page.tsx`, `app/pin/...`) เพื่อให้เมนูบน Header แสดงผลสอดคล้องกันทุกหน้า