# Worklog - 2026-03-02

## สรุปงานที่ทำวันนี้

1. ตั้งค่า MCP `context7`
- เพิ่มการตั้งค่า `mcp_servers.context7` ใน user config ให้ใช้ `npx @upstash/context7-mcp --api-key ...`
- ทดสอบเรียก Context7 API แล้วได้ผลลัพธ์กลับตามปกติ

2. ปรับส่วนหัวเอกสารใบเสนอราคา (บริษัท/ที่อยู่/ติดต่อ)
- ปรับข้อความข้อมูลบริษัทและที่อยู่ให้เป็นข้อมูลล่าสุด
- จัดเลย์เอาต์ให้ข้อมูลติดต่อแสดงตามรูปแบบที่ต้องการ
- บังคับช่วง `เลขประจำตัวผู้เสียภาษี 0105554023435` ไม่ให้ตัดกลางคำ

3. ปรับขนาดฟอนต์ในหน้า PDF และหน้าอนุมัติ
- ปรับขนาดฟอนต์หลายรอบตามคำขอ (เพิ่ม/ลด)
- ย้อนกลับการเพิ่มฟอนต์ทั้งหน้าแบบ global ที่ไม่ต้องการ
- ปัจจุบันคงการปรับฟอนต์เฉพาะหน้า PDF ตามคำขอล่าสุด

4. ปรับการใช้ฟอนต์
- ตั้ง `TH Sarabun New` เฉพาะหน้า PDF
- คืนฟอนต์หน้า UI ปกติให้กลับไปใช้ฟอนต์เดิมของโปรเจกต์

5. แก้ปัญหาโลโก้ใน PDF ไม่แสดง
- เปลี่ยนการอ้างโลโก้ให้โหลดจาก asset ของ Next build (`@/public/jjsats-logo.png`) แล้วแปลงเป็น data URL
- ทำให้โลโก้ `jjsats` กลับมาแสดงใน PDF ได้เสถียรขึ้น

## ไฟล์หลักที่เกี่ยวข้อง

- `app/page.tsx`
- `app/approve/[id]/approve.css`
- `app/approve/[id]/page.tsx`
- `app/globals.css`
