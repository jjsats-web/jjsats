# WORKLOG-2026-01-22

- ปรับ Bottom Navigation ให้ใช้งานในหน้าต่างๆ เพิ่มใน `app/pin/manage/page.tsx`
- เอาเมนูบน header ออกจาก `app/product/page.tsx` และ `app/pin/manage/page.tsx` ให้เหลือแค่แบรนด์
- เอาปุ่ม “กลับ” ออกจาก `app/pin/register/page.tsx`
- เปลี่ยนโทนสีหลักของระบบเป็นสีแดงเลือดหมู `#741010` ผ่าน `app/globals.css`
- แก้ปัญหาไอคอน Bottom Navigation แสดงเป็นข้อความบนมือถือ โดยเปลี่ยนไปใช้ SVG (`components/Icon.tsx`)
- ปิด prefetch สำหรับลิงก์ “ออกจากระบบ” ในเมนู เพื่อกันเด้งกลับหน้า PIN โดยไม่ตั้งใจ
