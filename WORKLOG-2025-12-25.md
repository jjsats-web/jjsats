# Worklog - 2025-12-25

- รัน `npm run gen:types` เพื่ออัปเดต Supabase types (`lib/supabase/database.types.ts`)
- เปลี่ยนพรีวิวลายเซ็นในหน้าตั้ง PIN ให้ใช้ `next/image`
- ปรับสีจุด PIN ที่กรอกแล้วให้เป็นแดงเลือดหมู (`#741010`)
- ปรับ hover เมนู header ให้เป็นกรอบแดงอย่างเดียว (ไม่เปลี่ยนพื้นหลัง/เงา)
- เปลี่ยนปุ่ม “เลือกสินค้า” (คลาส `.ghost`) ให้เป็นสีแดงเลือดหมู
- ปรับสีหัวตารางใน PDF (รายละเอียด/จำนวน/ราคา/รวม) เป็นแดงเลือดหมู
