Worklog - 2026-01-15

- เพิ่ม PWA: manifest, service worker, icon set และลงทะเบียน SW เฉพาะ production (ไฟล์ `public/manifest.webmanifest`, `public/sw.js`, `public/icons/...`, `components/ServiceWorkerRegister.tsx`, `app/layout.tsx`).
- แก้ `proxy.ts` ให้ bypass `manifest.webmanifest`, `sw.js`, และ `/icons/` เพื่อไม่ถูก redirect ไปหน้า PIN.
- รัน `npm run build` สำเร็จ.
- สตาร์ท `npm start` ที่พอร์ต 3001 เพื่อทดสอบ และจัดการพอร์ตชนกันโดยหยุดโปรเซสเดิม.

ค้าง/ต่อพรุ่งนี้:
- เช็ค `http://localhost:3001/manifest.webmanifest` ต้องเป็น JSON.
- ตรวจ DevTools → Application → Manifest/Service Workers ว่า register แล้ว.
