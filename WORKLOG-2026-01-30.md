# WORKLOG-2026-01-30

- แก้ปัญหา PIN เด้งซ้ำโดยปรับ `proxy.ts` ให้ข้าม prefetch และบังคับ no-store, พร้อมกัน cache จาก middleware
- ปรับ `BottomNav` และลิงก์เมนูหลักให้ปิด prefetch เพื่อไม่ให้ cache redirect เก่า
- เพิ่ม/แก้ prefetch ในหน้า `/`, `/customer`, `/product`, `/pin/manage`, `/pin/register`
- ลบ `middleware.ts` ที่ชนกับ `proxy.ts` ตามข้อกำหนด Next.js ใหม่
