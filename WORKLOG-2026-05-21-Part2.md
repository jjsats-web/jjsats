# Worklog: 2026-05-21 (Part 2)

## ภาพรวม (Overview)
บันทึกการทำงานส่วนที่ 2 ของวันนี้ เน้นไปที่การพัฒนาระบบ Interactive ให้กับหน้าสร้างใบเสนอราคา (Quotation V2) เพื่อให้สามารถค้นหาและดึงข้อมูลจาก API มาแสดงผลได้จริง พร้อมทั้งพัฒนาระบบตะกร้าสินค้า สรุปยอด และการบันทึกประวัติการทำใบเสนอราคาให้อยู่ทนทานต่อการรีเฟรชหน้าเว็บ

## สิ่งที่ดำเนินการไป (Tasks Completed)

### 1. ระบบค้นหาและเลือกลูกค้าแบบ Interactive
- เปลี่ยนช่อง "ค้นหาชื่อบริษัท" ให้กลายเป็นระบบ Autocomplete Dropdown
- เชื่อมต่อ API (`/api/customers`) เพื่อดึงข้อมูลลูกค้าในฐานข้อมูลมาแสดงแบบ Real-time ตามคำค้นหา
- เมื่อกดเลือกลูกค้า ข้อมูลบนการ์ดสรุป (ชื่อบริษัท, เลขผู้เสียภาษี, ผู้ติดต่อ, ที่อยู่) จะถูกแทนที่ด้วยข้อมูลจริงของลูกค้านั้นๆ ทันที
- นำปุ่ม "เลือก" เดิมในหน้า `/customer` ออก เพื่อลดความซ้ำซ้อนของ UX (เหลือเฉพาะปุ่ม "แก้ไข")

### 2. ระบบค้นหาและเลือกสินค้าแบบ 3 ระดับราคา
- เปลี่ยนช่องค้นหาสินค้าให้เป็นระบบ Autocomplete โดยดึงข้อมูลจาก `/api/products`
- **ซ่อนราคาในผลการค้นหา:** ตามความต้องการของผู้ใช้ เพื่อไม่ให้รกสายตา
- **ระบบเลือกประเภทราคา (Tier Selection):** เมื่อคลิกเลือกสินค้า จะมีเมนูย่อยปรากฏขึ้นมาให้เลือกว่าจะใช้ราคาไหน (User, Project หรือ Dealer) ก่อนที่จะเพิ่มสินค้านั้นลงตาราง
- **เพิ่มสินค้าใหม่แบบ Manual:** ปรับปรุงปุ่ม "เพิ่มสินค้าใหม่" ให้กางฟอร์มขนาดเล็กออกมา ให้สามารถพิมพ์ชื่อสินค้าและราคาเพื่อนำเข้าตารางใบเสนอราคาได้โดยตรง (ระบุหมายเหตุว่า "เพิ่มเอง")

### 3. ระบบคำนวณและตะกร้าใบเสนอราคา
- เปลี่ยนข้อมูล Mock แบบ Static (เช่น Notebook Pro) ให้ผูกกับ State `quoteItems` จริง
- ผู้ใช้สามารถกดแก้ จำนวน (QTY), แก้ไขราคาต่อหน่วย และแก้ไขส่วนลดเป็นเปอร์เซ็นต์ (%) ได้โดยตรงจากในตาราง
- เพิ่มตารางสรุปยอดด้านล่าง แสดง "รวมเป็นเงิน", "ส่วนลดรวม" และ "ยอดรวมสุทธิ" ซึ่งจะคำนวณใหม่แบบ Real-time ทุกครั้งที่มีการแก้ตัวเลขในตาราง

### 4. ระบบยืนยันการบันทึกใบเสนอราคา (Summary Modal)
- เมื่อกดปุ่ม "บันทึกใบเสนอราคา" จะแสดงหน้าต่าง Popup 
- ใน Popup จะสรุปภาพรวมทั้งหมด: ชื่อลูกค้า, รายการสินค้าและราคา, ยอดรวมสุทธิ
- เมื่อกดยืนยันการบันทึก ระบบจะล้างค่า (Clear Form) และสร้าง History จำลองขึ้นมา พร้อมกับเลื่อนหน้าจออัตโนมัติไปยังส่วนประวัติด้านล่าง

### 5. การจัดการสิทธิ์ผู้ใช้งาน (Role-based UI)
- ปรับเงื่อนไขการแสดงผล Header Menu: หากผู้ใช้เข้าสู่ระบบในฐานะ `user` ทั่วไป จะไม่สามารถเห็นหรือเข้าถึงเมนู "ใบเสนอราคา" (เวอร์ชันเก่า) และ "ทะเบียนลูกค้า" ได้ (กำหนด `adminOnly: true`)

### 6. การบันทึกข้อมูล History แบบ Persist (รอดการ Refresh)
- แก้ไขปัญหาประวัติใบเสนอราคาจำลอง (Mock History) ที่หายไปเมื่อกด F5 หรือรีเฟรชหน้าเว็บ
- นำ `localStorage` มาใช้จัดเก็บตัวแปร `savedQuotes` และ `approvalRequested`
- ทำให้เมื่อเปิดหน้าเว็บใหม่ ประวัติที่เคยกดบันทึกไว้ในเบราว์เซอร์เครื่องนั้นก็จะยังคงแสดงอยู่ครบถ้วน