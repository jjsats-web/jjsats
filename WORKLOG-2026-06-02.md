# Worklog 2026-06-02

## Quotation page

- Fixed quotation save on other machines by replacing the invalid non-UUID fallback with a valid UUID v4 fallback.
- Added a persistent note field on `/quotation` for "หมายเหตุ / ข้อเสนอและเงื่อนไข".
- Saved the note to `localStorage` so the next quotation can reuse it without retyping.
- Sent the note to `/api/quotes` so it appears in the quotation PDF section.
- Added a success popup after saving a quotation.
- Improved the approval request success popup to match the JJSATs theme.
- Fixed production approval flow so a popup rendering issue cannot show a false error after Telegram approval request succeeds.
- Updated quote history cards to show company name above quotation number.
- Made quote history cards expandable inline instead of navigating to the approval page.
- Inline quote details now show company name, quotation number, all items, item prices, and total.
- Added a delete button to each quote history item.
- Added a confirmation popup before deleting a quote.

## API

- Updated `DELETE /api/quotes/[id]` to remove related `quote_approvals` records before deleting the quote.
- Kept approval request status handling compatible with both `pending` and `approved` responses.

## Styling

- Added SweetAlert styles for JJSATs success, approval, error, and cancel buttons.
- Adjusted popup buttons away from the default blue SweetAlert style.
- Kept the approval popup visually aligned with the red JJSATs theme.
- Added mobile viewport protection to reduce pinch zoom, double-tap zoom, horizontal overflow, and overscroll bounce.

## Quotation PDF layout

- Adjusted the quotation customer/info box spacing so top and bottom padding are balanced.
- Tuned the customer/info box label and value line-height for a more even table rhythm.
- Updated both the PDF export template and the approval preview CSS so the layout matches in both places.
- Adjusted product table cells so product codes and descriptions align closer to the top instead of sitting too close to the bottom border.
- Set product table body cells to top vertical alignment with balanced padding and slightly tighter line-height.

## Mobile PIN login

- Locked `/pin` into a full-screen fixed mobile shell so the page cannot scroll up/down behind the PIN keypad.
- Added a scoped runtime screen lock only while the PIN page is mounted, then removes it when leaving the page so quotation/form pages are not affected.
- Disabled long-press context menu, text selection, drag selection, and iOS touch callout on the PIN screen.
- Added safe-area aware padding for mobile devices with notches/home indicators.
- Kept the PIN card centered on mobile and added compact sizing for short screens.
- Added a landscape compact layout so the PIN keypad stays usable without vertical scrolling.

## Verification

- Ran ESLint on changed files during the work.
- Ran `npm run build` after major changes to confirm the Next.js production build passes.
