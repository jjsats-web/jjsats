# Worklog - 2026-03-04

## สรุปงานที่ทำวันนี้

1. ปรับข้อความและลำดับข้อมูลในเอกสาร PDF
- เปลี่ยน `เรียน/ATTN` เป็น `บริษัท/Company`
- สลับบรรทัด `ลูกค้า/Customer:` กับ `บริษัท/Company`
- สลับบรรทัด `เลขประจำตัวผู้เสียภาษี (TaxID):` กับ `ที่อยู่/Address:` พร้อมย้ายข้อมูลตามหัวข้อ
- เปลี่ยนหัวข้อเลขเอกสารจาก `ใบเสนอราคาเลขที่/No.` เป็น `เลขที่/QT No.`

2. เพิ่มข้อมูลติดต่อในส่วนท้ายหน้า PDF
- เพิ่มบรรทัด `โทรศัพท์/Tel:`
- เพิ่มบรรทัด `อีเมล/E-mail:`
- เพิ่มบรรทัด `ยืนยันราคา/Valid Untill:` และกำหนดค่าคงที่ `15 วัน` ทุกใบเสนอราคา

3. ปรับโครงสร้างหัวตารางรายการสินค้าใน PDF
- เพิ่มคอลัมน์แรก `ลำดับ` และวาง `No.` ไว้บรรทัดล่าง
- ปรับหัวคอลัมน์ให้เป็นไทยบรรทัดบน/อังกฤษบรรทัดล่าง:
  - `รายละเอียด / Description`
  - `จำนวน / Unit`
  - `หน่วย / QTY`
  - `ราคา/หน่วย / Untill price`
  - `ราคารวม / Amount`
- จัดหัวตารางให้อยู่กึ่งกลาง

4. ปรับข้อมูลลูกค้าและ Supabase
- เพิ่มช่องกรอก `E-mail` ในหน้า `app/customer/page.tsx`
- อัปเดต API ลูกค้าให้รองรับ `contact_email`
- อัปเดต generated types ของ Supabase
- จัดทำ SQL สำหรับเพิ่มคอลัมน์ใน Supabase:
  - `scripts/add-contact-email-to-customers.sql`

5. แก้ปัญหา error จากไฟล์ generated/การตรวจโค้ด
- ปรับการตั้งค่า lint ให้ลดปัญหาจากไฟล์ generated ที่ไม่เกี่ยวกับงานหลัก
- รัน `npm run lint` ผ่าน
- รัน `npm run build` ผ่าน

## ไฟล์หลักที่แก้ (ตามงานวันนี้)

- `app/page.tsx`
- `app/approve/[id]/page.tsx`
- `app/approve/[id]/approve.css`
- `app/customer/page.tsx`
- `app/api/customers/route.ts`
- `app/api/customers/[id]/route.ts`
- `lib/supabase/database.types.ts`
- `scripts/add-contact-email-to-customers.sql`
- `eslint.config.mjs`
